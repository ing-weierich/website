# Google-Analytics-Measurement-ID (Format G-XXXXXXXXXX).
# Ohne Wert bleibt das Tracking komplett aus — gtag.js wird dann nicht geladen.
# Für den Produktions-Build als Repository-Variable/Secret in der GitHub Action
# oder lokal in einer .env-Datei setzen.
PUBLIC_GA_MEASUREMENT_ID=
